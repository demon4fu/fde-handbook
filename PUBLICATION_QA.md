# 公开发布检查说明

检查日期：2026-09-12。对象：本仓库 v1.0.0 的 PDF 与 LaTeX 源包。

- PDF 为 273 页，与当前完整版逐字节一致；本次上传未删减或改写正文。
- PDF 中文文本可提取，字体资源均嵌入，无附加文件。
- 源包包含 54 个文件，`main.tex` 位于根目录。实际使用的章节、样式、图像、字体与字体许可证随包提供。
- 从独立构建副本完成两轮编译；未将编译日志、缓存、工作目录、历史版本或未使用的素材打包。
- 对文本、PDF 元数据、链接及源包进行隐私检查，未发现已检查范围内的私人账号标识、本机绝对路径或凭据。

隐私检查是面向本次发布文件的合理审查，不是对所有可能推断身份信息的绝对保证。书中的技术、合规与部署边界以各章说明为准。

两项发行文件的 SHA-256 见 `SHA256SUMS.txt`。
